services:
  backend:
    build: ./be  # Đường dẫn đến thư mục chứa Dockerfile của Java
    ports:
      - "8080:8080"
  frontend:
    build: ./fe  # Đường dẫn đến thư mục chứa Vite
    ports:
      - "5173:5173"
    depends_on:
      - backend